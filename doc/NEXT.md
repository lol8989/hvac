# NEXT — 다음 작업 큐

> 세션 시작 시 이 파일부터 확인한다. 완료한 항목은 지우지 말고 `[x]` + 완료일·커밋을 남기고, 새 항목은 위에 추가한다.

## ▶ 2026-07-20 — 실명 추출 연동: 개발팀 회신 대기

> 개발팀이 **도면에서 실명을 뽑아내는 기능**을 개발 중(주인님 전달 2026-07-20).
> 명세·질문 3건: [`05_설계결정/실명_추출_연동_명세_v1.md`](05_설계결정/실명_추출_연동_명세_v1.md)

**한 것 (커밋됨):**
- [x] 층별 천정고 입력 — 4m 이상 특수부하 일괄 적용 (2026-07-20, `0f711d5`)
- [x] 폴백 관측 — `usageResolution.ts` (exact/normalized/alias/unknown/unnamed) + 선정표 배지 (2026-07-20)
      실측: 목업 6실 중 **4실이 표에 없어 150으로 폴백**(거실·침실1·로비·탕비실) — 이전엔 화면에 안 보였다.

**개발팀 회신 대기 (3건):**
1. [ ] 추출기 출력이 날 텍스트인가, 정규화된 실명인가 → 우리 쪽 `normalizeUsage` 존치 여부
2. [ ] 무명 실을 구분해 넘기는가(빈 문자열/null/'미상') → `unnamed`/`unknown` 분기가 여기 달렸다
3. [ ] 라벨 후보가 여럿일 때 확정해 주는가 → 계약을 `usage: string[]`로 넓힐지

**회신 후:**
4. [ ] 실측으로 `unknown` 실명·빈도 수집 → 현업이 매핑 확정 (**개발이 임의 매핑 금지** — 단위부하는 장비 용량 직결)
5. [ ] `ALIASES`를 시설군별 사전으로 확장 (`Record<FacilityType, Record<string,string>>` + 전역 폴백)
6. [ ] (보류) 복합건물 층별 시설군 오버라이드 — 실측 후 필요성 판단

## ▶ 2026-07-20 — 조합관리 축 + FCU 제외 + 현업 회신 전량 반영 **완료**

> 아래 '월요일 핸드오프' 절은 **낡았다** — WIP는 `e38606c`로 이미 커밋됐고, 현업 회신 엑셀도
> 7/16에 받아 반영됐다(격자는 원본과 바이트 단위 동일). 남은 건 데이터가 아니라 축 매핑이다.

**한 것 (2026-07-20):**
- [x] 테스트 갱신 — 축 개수 고정(35×39) 제거, 의미 단언으로 대체
- [x] **신규 시리즈 자동 등장 회귀 테스트** 추가 (카탈로그에 INSERT → 행 자동 등장 + 기본 X). 의도대로 동작 확인
- [x] `cleanSeedLabel`이 카탈로그 이름과 맞는지 확인 → **안 맞는다.** 실측: 카탈로그 1368칸 중 **318칸(23%)이 현업 근거 없이 기본 X**
      원인은 시리즈명이 아니라 **중분류 명칭 체계**가 시드(현업 표기)와 카탈로그(우리 분류)에서 다른 것.
      값이 뒤집힌 칸은 0 — 축 이름만 안 맞는다. 상세: [조합확인표 문서 §6](05_설계결정/실내외기_조합_확인표_현업회신_반영_2026-07-16.md)
- [x] 래칫 테스트로 고정 — 근거 못 받는 열 목록이 늘면 즉시 깨진다

**한 것 (이어서) — 매핑 구현:**
- [x] 중분류가 어긋나면 **시리즈명으로 되짚기** (`seedValueAt`). 후보 유일 또는 값 만장일치일 때만 채택,
      갈리면 null(임의로 고르면 현업 판정 날조). → **근거 못 받는 실내기 열 8개 → 0개**
- [x] 래칫 테스트 정상 경로대로 축소 (고아 열 0 / 미근거 칸은 큐레이션 냉방전용 행에만)

**한 것 (이어서) — 큐레이션 조사 + FCU 제외:**
- [x] `냉방전용 | Multi V 실외기(큐레이션)` 정체 규명 → **모델 0개 빈 시리즈**. 현업 문의 대상 아님
- [x] 시드 생성기가 **모델 0개 시리즈를 싣지 않게** 수정(`buildSpecSeed.ts`). 유령 축 3건 제거
      (냉방전용|큐레이션 · 2WAY 카세트|민수전용 · 벽걸이형|Multi V S(주거))
      → **카탈로그 전 칸이 현업 확정값을 받는다(미근거 칸 0)**
- [x] **FCU 장비마스터 적재 제외** (주인님 결정 2026-07-20, 현업 회신 질문 2). 7시트·51모델 미적재.
      조합관리 축에서도 사라짐(브라우저 확인). 생성단 방어 규칙(`indoorCombinability.ts`)은 존치
- [x] 의도된 제외(`excluded`)를 '분류 불가'(`skipped`)와 분리 — 커버리지 검사 거짓 경보 제거

**한 것 (이어서) — 조합표 정본 기준 정정 (주인님 확인: 근거자료 탭은 우리가 만든 것, 조합표가 정본):**
- [x] **`Multi V S(주거)` 실외기 → 냉방전용**. 근거 3가지 일치 — 현업 조합표 주석 + 모델코드 RPU`Q` +
      **스펙시트 난방용량 0/14**(다른 절환형 시리즈는 100% 있음). 난방 필요 실에 배정 안 됨
- [x] **듀얼베인 실내기 → 1WAY**(현업 `(4way->1way수정)`). Multi V S(주거)에만 적용, 다른 라인업은 4WAY 유지
- [x] **천장형 실내기 → `천장형`**(시트명 `실내기-천장형`인데 규칙이 없어 `기타 실내기`로 뭉쳐 있었다)
- [x] 회귀 테스트 `seed/seedMultiVsResidential.test.ts` 6건 (난방용량 대조군 포함)

> **현업 확인 대기 항목 없음** — 7/16 회신에서 받은 답변은 전부 반영됐다.

**참고 — 게시 모델이 26개뿐**: 실내기 19(전부 큐레이션) · 실외기 7. 전체 1356 중 DRAFT 1329.
생성·검도는 PUBLISHED만 보므로 **실외기 선정 후보가 7개**다. 선정 품질 평가 시 감안.

상세: [조합확인표 문서 §6·§7](05_설계결정/실내외기_조합_확인표_현업회신_반영_2026-07-16.md)

## ▶ (낡음) 월요일(2026-07-20) 이어서 — 조합관리 "카탈로그-파생 축" 미완 WIP

> 상태: **미커밋 WIP 3파일**. tsc 클린이지만 **테스트 미갱신 → 실패 예상**. 목표: 조합관리 매트릭스가
> 게시 카탈로그(product_series)에서 축을 파생 → 장비마스터에 **새 시리즈가 생기면 조합관리에 자동 등장**.
> (주인님 승인 2026-07-16. 배경: 목록관리→조합비정책은 이미 자동 연결, 조합관리만 축이 시드 고정이었다.)

**한 것(미커밋):**
- `infrastructure/equipment/sqlite/readCompatMatrix.ts` — 재작성. 축=카탈로그(OUTDOOR 행·INDOOR 열, `AXES_SQL` GROUP BY 중분류·시리즈). 값 = override ?? `compatDefaultValue`(=시드 ?? 'X'). 카탈로그 비면 `compatMatrixFromSeed` 폴백. `compatAxisExists`·`compatDefaultValue` export 추가.
- `SqliteEquipmentAdminRepository.ts` — `setCompatCell` 축 검증을 시드→**카탈로그**(`compatAxisExists`)로, 삭제 판정을 시드값→**실효 기본값**(`compatDefaultValue`)으로. import 정리(seedValueAt 제거).
- `compatMatrixFromSeed.ts` — `seedValueAt`가 시드 라벨의 현업 주석을 떼고 조회(`cleanSeedLabel`): "Multi V S(주거)->..." → "Multi V S(주거)", "천장형(확인요망)" → "천장형" 등 5개. 정당한 괄호((고급형) 등)는 보존.

**남은 것 (월요일):**
1. [ ] `SqliteEquipmentAdminRepository.compat.test.ts` 갱신 — 현재 35×39 고정 단언이 깨진다. 카탈로그-파생 기준으로: 정확한 개수 대신 **GHP Super III × 대공간덕트=O**·GHP×4WAY=false·수냉칠러 전부 false 등 의미 단언으로. delete-on-equal은 실효 기본값(compatDefaultValue) 기준.
2. [ ] **신규 시리즈 자동 등장** 회귀 테스트 — 테스트 DB에 새 product_series(OUTDOOR) 1개 INSERT → `getCompatMatrix().outdoorRows`에 등장 + 기본값 X 확인.
3. [ ] `cleanSeedLabel` 정규화가 5개 주석 라벨을 카탈로그 실제 이름과 맞추는지 확인(실제 카탈로그 series명 조회해 대조). 안 맞으면 override 시드로 보정하거나 매핑.
4. [ ] 전체 스위트·tsc·build 그린 확인 → 브라우저에서 조합관리 렌더(카탈로그 축, GHP 예외 유지) 재검증 → 커밋.
5. [ ] (선택) 성능 — seedValueAt이 카탈로그 셀마다 findIndex×2. 축이 커지면 시드 룩업 맵 프리컴퓨트 고려.

**되돌리려면**: `git checkout -- src/infrastructure/equipment/sqlite/readCompatMatrix.ts src/infrastructure/equipment/sqlite/SqliteEquipmentAdminRepository.ts src/infrastructure/equipment/seed/compatMatrixFromSeed.ts` (안내문 일반화 6a0ef8e는 유지).

### 그 밖의 열린 항목(우선순위, "생성+관리자 완성" 마감)
- [ ] **이격거리 좌표계** — `snap()` GRID 상수가 좌표계 무관 + `mmPerUnit`(타일) 부재로 실외기 이격(200/900mm) 검증이 목업에서 미발화. 레퍼런스로선 실외기 배치 검증이 잠들어 있음. (`clearanceRules.ts`, `App.tsx:857-865`)
- [ ] QA 잔여: 심볼 실 밖 드래그 검증 없음 / 산출 도면 실 라벨 centroid 겹침.
- [x] **생성 파이프라인 스텝1~4 end-to-end 검증** (2026-07-16) — 배치→선정(6/6·조합비0.81)→실외기 자동배치(1/1)→산출물. 선정표(23컬럼·집계 정상)·일람표(SQLite 롱테일 실측값, '-'는 스펙시트 부재분만)·도면 SVG 모두 정상. 앱 콘솔 에러 0.

## 2026-07-16 — 현업 조합확인표 회신 반영 + 실내외기 조합관리

> 근거: [`doc/실내기_실외기_조합_확인표_260716_현업회신.xlsx`](실내기_실외기_조합_확인표_260716_현업회신.xlsx), 검토·결정 [`doc/05_설계결정/실내외기_조합_확인표_현업회신_반영_2026-07-16.md`](05_설계결정/실내외기_조합_확인표_현업회신_반영_2026-07-16.md)
> 현업은 격자(O/X 가정)를 수용하고 질문 5건에만 회신. 격자는 사실상 "EHP·VRF ↔ Multi V 실내기 = O, 그 외 계열 = X/–"로 수렴(우리 계열+is_vrf 규칙과 대체로 일치)하나, GHP↔대공간덕트·냉방전용 전용실내기 등 시리즈 예외가 있어 기준데이터화가 정당.

- [x] **Stage A — FCU 제외 + 냉매 불변식** (2026-07-16, 미커밋) — `domain/generation/indoorCombinability.ts`(신규): FCU(물 기반)를 생성 실내기 풀에서 제외(질문 2). 냉매 종류(R32/R410A)로 조합을 가르지 않음(질문 1)을 주석·테스트로 고정. `InMemoryIndoorModelCatalog`가 필터 적용.
- [x] **B1 시드** — `infrastructure/equipment/seed/compatMatrixSeed.ts`: 회신 xlsx에서 추출한 확정 조합표(실외기 35 × 실내기 39, 값 O/X/-/D).
- [x] **B2 도메인** — `domain/equipment/CompatMatrix.ts`: 불변 VO(축=중분류·시리즈, valueAt/isCompatible/withValue). 조립 팩토리 `compatMatrixFromSeed`(infra).
- [x] **B3 관리자 조합관리 페이지** — 스키마 v6 `series_compat`(override 전용, 기본=시드) + admin 포트 `getCompatMatrix`/`setCompatCell`/`clearCompatForOutdoor` + AdminShell 메뉴 `?view=compat` + main 라우팅. 전체 1130 그린·tsc·build 클린.
  - **화면은 마스터·디테일**(주인님 지시 2026-07-16): 격자(엑셀 시트 복사)가 아니라 좌: 실외기 시리즈 목록(계열 그룹·검색) / 우: 그 시리즈의 연결 가능 실내기 유형 체크리스트(중분류 묶음)·확정 기본값 복원. 브라우저 실검증(선택·검색·토글→저장→리로드 유지·복원). `CompatMatrixPage.tsx`.
  - **적대적 코드리뷰(38에이전트) 21건 반영**: 구분자 AXIS_SEP 통일, setCompatCell 시드=값이면 삭제, 중복축 거부, 경량 검증, 접근성·14px, 토스트 타이머 등. 격자 특화 지적은 마스터·디테일 재설계로 자연 해소.
- [x] **B4 — 생성단 조합표 소비 + 버킷 재설계** (2026-07-16, 미커밋) — `selectOutdoorUnits`를 `층×계열` 버킷에서 **`층`-only + 호환 predicate**로 재설계. 기본 predicate=계열(`byEnergySource`)이라 기존 21테스트 그대로 통과. 조합표(CompatMatrix) 주입 시 시리즈×유형 호환을 따르고 계열 교차(GHP↔대공간덕트)도 성립. 라벨을 4개 레이어에 threading(`OutdoorCandidate`/`IndoorForSelection`/`IndoorUnit`/`planAdapter`), `compatPredicateFromMatrix`(하이브리드: 조합표 없는 축은 계열 폴백), App/main.tsx 주입(SQLite면 시드+override, 폴백이면 시드). 브라우저 실검증(6실 배치→실외기 1대·배정 6/6·조합비 0.81, 무회귀). 전체 1138 그린·tsc·build 클린. 신규 단위테스트: 계열 교차 enable·X-block·serves-all·no-compat.
  - [ ] **SelectionTable 계열 교차 대응** — 장비선정표/일람표가 `층×계열`로 소섹션을 나눈다(`SelectionTable.ts`). 계열 교차 그룹(GHP 실외기 + EHP 실내기)이 실제로 생기면(현 목업엔 없음, 실도면 DXF에서) 그 소섹션 귀속을 재검토해야 한다. 지금은 미발현이라 무회귀.
- [ ] **B4 후속 — SINGLE(단품) 1:1 + product_combinations** (질문 3·5) — 단품은 1:1, 능력은 세트 단위. `product_combinations` 적재 + T/P/V/B 앞글자 유형 분류(T=카세트·P=스탠드·V=상업용 천장형·B=덕트형). 현재 단품 실외기는 is_vrf=0로 조합 후보에서 제외돼 있음(오작동 아님).
- [ ] 민수/조달 실내기 중복 라인 dedup(질문 4: 스펙 동일·거의 안 섞음) — 경미.

## 2026-07-15 — 층 전환(Floor Switching)

> 설계·근거: [`doc/05_설계결정/층_전환_설계_v1.md`](05_설계결정/층_전환_설계_v1.md)
> 배경: 다층·다실을 좌측 상주 리스트(개발자 버전 반례)가 아니라 **캔버스가 한 번에 한 층**으로 푼다. 도면 중심 유지.

- [x] 층 전환 설계 v1 작성 (2026-07-15, 미커밋)
- [x] `domain/generation/floorOrder.ts` — 층 정렬(지하/지상/옥탑/이상치) + 테스트 7/7 (2026-07-15, 미커밋)
- [x] presentation `floorsOf` — 정렬 층 목록 + 층별 bbox 파생 + 테스트 5/5 (2026-07-15, 미커밋)
- [x] `Viewer` fitBounds prop(활성 층 bbox fit) + `App` activeFloor 필터 렌더 + 층 탭 UI(무채색, 뷰 상태) (2026-07-15, 미커밋). 단층이면 탭 숨김 → 무변화. 전체 1101 그린·tsc 클린
- [ ] **목업 다층화 + 영향 테스트 갱신** — 목업에 2층 추가 시 App 통합 테스트 20건(총계·미배정·실외기 선정)이 6실 고정값이라 깨진다. 다층 목업으로 진화 + 그 단언들 갱신 필요(또는 실 다층 DXF 도착 시 자동 활성). ← **결정 대기**
- [ ] 적대적 QA: 층 1개·층명 이상치·빈 층·층 간 실 이동 금지·전환 중 편집 스코프 누수
- [x] (오픈) 검출 워커 합의 — 층 나란히 배치 확정, 층 식별자는 우리 정의(`지하N층/지상N층/N층/옥탑`, `floorOrder.ts` 주석) (2026-07-15)

## 2026-07-14 — 실(Room) 슬라이싱 (V 도구)

> 설계·근거: [`doc/05_설계결정/실_슬라이싱_설계_v1.md`](05_설계결정/실_슬라이싱_설계_v1.md)
> 확정 결정 4건(주인님): ①실 형상을 **폴리곤으로 일반화** ②**검출 단계에서만** 자르기 ③배치된 실은 **심볼 위치로 자동 분배** ④자식 id는 **부모 파생**(`AC_001-1`)

- [x] 실 검출 결과가 화면에 안 나오던 버그 2건 수정 (2026-07-14, 미커밋)
  - Viewer의 `zones`가 마운트 시점 rooms로만 초기화돼, 검출된 실이 도면에 영원히 안 그려졌다
  - `detectRooms`가 같은 커밋에서 `setStep('place')`까지 해서 검출 결과 패널이 한 프레임도 안 보였다 → 검출 단계에 머물고 `실내기 배치 →` CTA로 전진
- [x] **P0~P8 실 슬라이싱 구현** (2026-07-14, 미커밋) — 테스트 1013 그린, tsc 클린, 브라우저 실검증(사선 120° 절단 → 면적 합 보존)
  - `domain/shared/Polygon.ts`(신규): shoelace 면적 · ray-casting 포함(경계 포함) · 최소면적 OBB · 반평면 클리핑 절단
  - `domain/generation/sliceRoom.ts`(신규): 자식 면적 = 부모 면적 × 넓이비(**면적 합 보존**), 단변/장변은 자식 OBB에서 재계산, 2% 미만 조각은 거부
  - **실 기하 SSOT를 `App.roomGeom`으로 승격** — 목업 `ROOMS`는 '검출기 출력'으로 격하. 이게 없으면 잘린 새 실은 좌표를 가질 자리가 없다
  - `layoutPositions` 폴리곤 일반화(좌표는 반드시 실 내부 — 벽 밖 심볼은 옆 실 소속으로 오분류된다)
  - 뷰어: V 진입 + 라인 커서(cursor:none) + R 15° 회전 + 하단 플로팅 메뉴 항목. **에어컨 모드 R(90° 회전)은 회귀 테스트로 보존**
  - 산출 도면도 `<polygon>` — 화면에서 자른 모양이 그대로 실린다
- [x] **적대적 QA 24건 확인 → 수정** (2026-07-14, 미커밋) — 5개 공격 관점(기하 경계·불변식·상호작용·좌표계·산출물 정합) 병렬 탐색 + 반증 검증
  - **[HIGH] 흰 화면 크래시 2건**: ①자른 자식 실을 선택한 채 재검출 → `aiSelectionFor(undefined)`가 렌더 중 터짐 ②선정표 새 창이 사라진 실을 편집 → 같은 경로. 해결: `liveSelRooms`(존재하는 실만) 파생 + 편집 커맨드의 실 존재 검증 + 검출 시 선택 비움
  - **[HIGH] 자르기 모드가 단계를 넘어 유지**: 게이트가 진입 시점에만 있어, 실내기 배치 단계에서도 클릭이 실을 잘랐다. 해결: 뷰어가 `canSliceRooms`가 꺼지면 모드에서 빠져나오고, App이 실행 직전에 단계를 다시 확인
  - **[HIGH] 얇은 실에서 `layoutPositions`가 raw Error → 루트 언마운트**: bbox 균등 샘플링이라 폴리곤이 bbox의 1%인 띠에서 내부점을 못 찾았다(자리는 실제로 있었다). 해결: 간격을 **폴리곤 넓이 기준**으로 잡고, 최후엔 OBB 장축 중심선을 따라 배치 + `RoomTooThinForUnitsError`(DomainError)로 승격 + App이 catch
  - **[HIGH] 뷰어 면적 라벨이 도메인과 10~42배 어긋남**: `zoneAreaM2`가 `mmPerUnit`(DXF 실좌표 축척)으로 목업 폴리곤 넓이를 환산했다 — 도면이 622㎡, 선정표가 31.9㎡. 해결: 면적은 **도메인 값만** 쓴다
  - **[HIGH] 존 리사이즈가 형상만 바꾸고 면적·부하·대수에 무기여**: 해결: `Room.withShape` 신설 — 실의 축척을 지키며 새 폴리곤에서 면적·단변·장변 재유도
  - **[HIGH] 선정표 셀 편집 한 번에 도면에서 옮긴 실내기 좌표가 초기화**: `withAiSelection`이 대수가 같아도 AI 좌표로 덮었다. 해결: 대수가 그대로면 좌표 유지
  - **[MEDIUM] 자르기가 실이 될 수 없는 조각을 만들었다**(폭 2cm·0.05㎡): 상대비(2%)만 봤다. 해결: 절대 면적 하한 1㎡ + 단변 하한 0.5m
  - **[MEDIUM] 자르기가 사용자 오버라이드를 AI로 강등** → 자식도 오버라이드 승계. **[MEDIUM]** 숨긴 레이어의 객체가 마퀴·Del·자르기로 편집됨 → `layerOn` 검사. **[MEDIUM]** 자르기 프리뷰 stale 커서 → 모드 변경 시 초기화. **[MEDIUM]** 도면 `viewBox`가 음수 좌표를 못 담아 왼쪽에 놓은 실외기가 산출 도면에서 사라짐 → min 기준으로 계산. **[MEDIUM]** CSV 면적이 `10.629999999999997` → 소수 1자리
  - **[LOW]** Esc가 자르기 모드를 못 벗어남 → 복귀. **[LOW]** 자르기 클릭이 격자 스냅 무시 → `gridStep` 스냅(스냅점이 실 밖이면 원점 사용). **[LOW]** 검출 패널 총 부하가 실별 반올림 합이라 자를수록 흘러내림 → 원값 합산 후 1회 반올림
- [ ] **[QA 잔여·백로그]** 심볼을 실 밖으로 드래그해도 검증이 없다(불변식 '심볼은 실 내부' 미강제) — 자르기 재분배가 그 상태를 상속
- [ ] **[QA 잔여·백로그]** `snap()`의 `GRID=20`이 좌표계 무관 상수 — 표시 격자(2m)와 스냅 격자(1.9m)가 어긋나고, 실외기 이격(200/900mm) 검증이 사실상 발화하지 못한다
- [ ] **[QA 잔여·백로그]** 타일(planDims)이 배치 이후 도착하면 심볼이 실과 어긋난다(재계산 경로 없음). Viewer 리마운트가 모드·각도를 초기화
- [ ] **[QA 잔여·백로그]** 산출 도면의 실 라벨이 centroid라 얇은 조각에서 겹친다
- [x] **실 병합(M) + 되돌리기/다시하기** (2026-07-14, 미커밋) — 설계: [`실_병합_되돌리기_설계_v1.md`](05_설계결정/실_병합_되돌리기_설계_v1.md)
  - **목업 실을 빈틈없는 인접 파티션으로 교체** — 실이 떨어져 있으면 병합 자체가 불가능했다. 픽셀 넓이를 면적에 비례시켜(≈1151 px²/㎡) 실내기 타입·대수·조합비는 그대로 유지
  - `Polygon.unionPolygons` — 경계 순회(공유 변은 반대 방향으로 나타난다)로 합집합. 결과는 오목할 수 있다(ㄴ자)
  - **`splitByLine` 오목 일반화** — 평면 그래프의 면(face) 추출. 볼록은 기존 반평면 클리핑 경로 유지(회귀 방지). 3조각 이상이면 자르기를 거부한다
  - `mergeRooms` — 면적 합 보존, 단변·장변은 합집합 OBB에서 재계산, 용도는 큰 실 승계(부하 변화를 숫자로 알림), 층이 다르면 거부, **형제를 합치면 부모 복원**(AC_001-1 + AC_001-2 → AC_001 거실)
  - **`World` 통합 + `useUndoable`** — 편집 상태 6개(실·형상·배치·실외기 좌표·조합플랜·시설군)를 한 스냅샷으로. Ctrl+Z / Ctrl+Shift+Z + WorkBar ↶↷ 버튼. **파생 동기화(syncPlanUnits)는 `replace`로 히스토리를 남기지 않는다**
  - 브라우저 실검증: 사무실+로비 → 97.0㎡ · 16.9kW(합 보존) → 되돌리기로 복원 / 회의실+탕비실(ㄱ자) → 다시 자르기 → 28.0 + 12.5 = 40.5㎡. 콘솔 에러 0
- [ ] 폴리곤 실의 **정점 리사이즈** (지금은 사각형 실만 모서리 핸들)
- [ ] 3개 이상 실 한 번에 병합(다중 선택) / 오목 실의 OBB 정확도(볼록 껍질 기준이라 과대평가)

## 2026-07-13 — 프로덕션 관계형 DB(PostgreSQL) 이관

> DDL(정본): [`doc/02_DB설계/03_schema_v3.sql`](02_DB설계/03_schema_v3.sql) · 근거: [`04_sqlite_to_pg_migration.md`](02_DB설계/04_sqlite_to_pg_migration.md)
> 대조 결과: PG DDL v2(2026-05-22)가 POC(SQLite v4)보다 **오래됐다.** POC가 그 뒤 붙인 조합·게시 규칙 컬럼 6개가 PG에 없어,
> v2를 그대로 세우면 생성(Generation) 도메인이 동작하지 않는다.
> **순서: 프로덕션 PG를 먼저 세우고, POC 적용은 그 뒤로 미룬다(주인님 지시 2026-07-13).**

- [x] SQLite ↔ PG 스키마 전수 대조 + 이관 설계 문서화 (2026-07-13, 미커밋)
- [x] 설계 결정 3건 확정 (2026-07-13) — 상세·근거는 문서 §7
  - 게시 요건(VRF 실외기 max_connections 필수) → **애플리케이션 단일 책임.** DB 트리거로 이중 방어하지 않는다
  - 단가 → **이관 범위 제외.** 스펙시트에 단가가 없다. 가격 테이블 구조만 유지, `product_prices.priority`는 PG에 안 넣는다
  - 중량·치수·소비전력 → **JSONB 유지, PG 정규화 컬럼 제거.** 채움률 과반 미만(소비전력 46.5%/38.1%), 값이 비스칼라(`"11 / - / -"`, `"860 x 132 x 450"`), 쿼리 조건 아님(이격거리도 상수 사용), SpecLookup이 이미 JSONB에서 읽음
- [x] **PG DDL v3 작성** (2026-07-13, 미커밋) — `03_schema_v3.sql`. 배포 가능한 self-contained DDL
- [ ] v3 DDL을 실제 PG 인스턴스에 적용 + `02_seed_v2.sql` 룩업 시드 적재(energy_sources 포함)
- [ ] 이관 사전 검증 쿼리 실행(NULL 타임스탬프·조합비 역전·JSON 파싱) — 전부 0이어야 시작 (문서 §5)
- [ ] energy_source 문자열 → `energy_sources.id` 변환 스크립트 (문서 §4-1)
- [ ] 1,206모델 × spec_data 적재 (`public/equipment-seed.json` → products + product_specs)
- [ ] **[POC 후속, 프로덕션 안정화 후]** FK 컬럼명 3개를 PG 명명으로 리네임 + `SCHEMA_VERSION` 4→5 (문서 §6)
  - 영향 파일 4개뿐: `schema.ts` / `seed.ts` / `SqliteEquipmentAdminRepository.ts` / `SqliteEquipmentMaster.ts` (테스트는 컬럼명 미참조 → 무영향)
  - POC의 `product_prices.priority` 제거도 이때 함께

## 2026-07-10 — 생성 모드 재구성 (P0~P5, 진행 중)

> 계기: "생성 메뉴의 목적은 장비를 배치해 **장비선정표·장비일람표**를 뽑는 것"(주인님)이라는 기준으로 적대적 평가.
> 확인된 결함 10건 중 셋이 목적과 정면 충돌 — 실외기 선정 알고리즘 부재, 실외기 배치가 산출물에 무기여, 도면 편집이 휘발성.
> 확정 결정: ①실외기 선정 = 층×계열 묶음 + 허용범위 만족 최소 용량 ②대수 SSOT = 도면 심볼
> ③스텝 순서 `검출→실내기→실외기 선정·조합→실외기 배치→산출물` + 좌표를 산출물에 반영 ④UI 전면 재편 ⑤가드는 CTA 활성 + 클릭 시 사유 팝업

- [x] **P0. 도메인 정합 수선** (2026-07-10, 미커밋) — 실외기 선정 알고리즘의 전제를 먼저 바로잡음.
  - **결함 A: 조합비가 세 곳에서 다르게 계산됐다.** `OutdoorGroup.comboRatio()`는 `Σ IndoorUnit.cool`을 쓰는데 `planAdapter`가 거기에 **실 설계부하**를 넣었다. 반면 `ratioOf`(리포트·매핑팝업)와 `SelectionTable`은 **정격×대수**를 썼다. 도메인 불변식·경고가 산출물과 다른 숫자를 보고 있었다.
  - **결함 B: 최대 연결 수를 '실' 개수로 셌다.** `IndoorUnit.id === roomId`라 실내기 2대짜리 방이 1대로 계산됐다.
  - 해결: **`IndoorUnit` 1개 = 실내기 1대**로 재정의(`id = ${roomId}#${n}`, `roomId` 필드 추가, `cool` = 모델 정격). 두 결함이 같은 뿌리에서 나왔고 함께 사라진다.
  - `AssignmentPlan` **교차 불변식 ② 신설**: 한 실의 모든 대수는 같은 곳에 있다(선정표의 실 행이 실외기를 하나로 특정해야 하므로). 이동 API를 `reassignRoom`(실 단위)으로만 노출 — 유닛 하나만 옮기면 불변식이 깨진다. `OutdoorGroup.split`도 실 단위 분할.
  - `ensureRoomsInPool` → **`syncPlanUnits`**: 배치(placements)가 SSOT이고 플랜이 따라간다. 모델 변경은 제자리 용량 갱신, 대수 변경은 여유 있으면 제자리 유지, 계열 변경·maxConn 초과·배치에서 삭제는 풀로 방출.
  - `data.ts`의 `ratioOf` **삭제** — 조합비는 도메인이 SSOT(`GroupView.ratio/judgement`).
  - 브라우저 검증: 실외기-2가 `연결 4대 (3실)`로 표기(로비 2대), 조합비 0.73/0.61이 `총 설치 용량 37.6kW`와 정합. 테스트 832 그린.
  - 잔여: 매핑 팝업 칩이 `ROOMS` 설계부하를 표시해 정격 기준 조합비와 숫자가 안 맞아 보인다 → P5에서 `ROOMS` 직접 import 제거와 함께 해소.
- [x] **P1. 대수 SSOT를 도면 심볼로** (2026-07-10, 미커밋) — 도면 심볼 하나 = 실내기 한 대 = 선정표 대수 1.
  - **결함: 뷰어 편집이 휘발성이었다.** `Viewer`가 `symbols`를 로컬 state로 쥐고 App에 올리지 않아, Del로 지워도 선정표 대수는 그대로였고 `＋실내기`가 만든 `IDU_*` 고아 심볼은 아예 표에 안 실렸다. 게다가 `placeUnits`는 **실당 심볼 1개**만 그려 대수 2 이상인 실을 시각적으로 속였다.
  - `Placement`에 `positions[]` 편입 + **불변식 `positions.length === effectiveSelection.quantity`**. 이 불변식이 "도면에서 지웠는데 표엔 남아 있는" 상태를 표현 불가능하게 만든다. `moveUnit`/`rotateUnit`(좌표만) vs `addUnit`/`removeUnit`(대수 변경 → 사용자 오버라이드)로 축을 분리. 마지막 한 대를 지우면 `removeUnit`이 `null`(그 실엔 실내기 없음).
  - `layoutPositions.ts` 신규(순수) — 실 사각형 + 대수 → 겹치지 않는 격자 좌표. `applyAiPlacement`가 `layoutFor` 주입받음.
  - `Viewer` 실내기 심볼 **controlled 전환**: `indoorSymbols` prop + `onUnitsMove/Rotate/Delete/Add`. 드래그·회전 중엔 로컬 draft로 그리고 **pointerup에 한 번만 커밋**(60fps 리렌더 방지). `UnitSym`에 `roomId` 추가, 선택 동기화도 실 기준으로.
  - `＋실내기`는 **선택한 실에 1대 추가**로 바뀜(자유 심볼 경로 제거) — 실에 안 묶인 실내기는 산출물에 실릴 수 없다. `ViewerHandle.placeUnits` 제거(AI 배치는 명령이 아니라 상태 갱신).
  - 브라우저 검증: AI 배치 → 심볼 7개/실 6곳(침실1이 2대). `AC_002#2` 선택 후 Del → 총 설치 용량 **37.6 → 35.6kW**. `＋실내기`로 되돌리면 **37.6kW** 복귀. 콘솔 에러 0. 테스트 857 그린.
- [x] **P2. 실외기 선정 알고리즘** (2026-07-10, 미커밋, 주인님 지목) — 실외기 대수·모델·매핑이 목업 상수였다(`INITIAL_GROUPS` 3대 + `DEFAULT_COMBINATION` 손매핑). 실 검출 결과가 달라지면 무의미했고, GHP인 ODU3는 계열 불일치로 영원히 빈 그룹이었다.
  - `domain/generation/selectOutdoorUnits.ts` 신규(순수). **층 × 계열 버킷 → 허용범위(모델별 `comboRange`, 기본 50~103%)를 만족하는 최소 용량 모델**. 최소 용량이 곧 목표 조합비 100%에 가장 가깝다(후보를 용량 오름차순 정렬 → 첫 적합이 답).
  - 분할: `maxConnections`(대수 기준)·40HP·상한 초과 시 First-Fit-Decreasing. **실 단위 묶음(RoomBundle)으로 담아 한 실의 대수가 쪼개지지 않는다.**
  - **발견: 계열만으로는 냉난방을 못 가른다.** 같은 EHP에 절환형과 냉방전용이 함께 있어, 목업 6실(정격 37.6kW)에 냉방전용 `RPUQ141X9S`(39.2kW)가 '최소 용량'으로 뽑혔다 → `requireHeating` 규칙 신설(기본 true, `heatKw === null` 배제). ⚠️ 지금은 항상 true 하드코딩 — 프로젝트 설정(냉방전용 건물)으로 올려야 한다.
  - 도메인 에러 `NoCompatibleOutdoorError`(계열에 후보 0건) / `UnpackableLoadError`(한 실을 어떤 실외기로도 못 담음).
  - `bootstrapPlan()`은 빈 플랜. `autoCombine` 삭제 → `selectOutdoorPlan` 어댑터. `autoCombinedRef`(세션당 1회) 제거 → "그룹이 없을 때만 1회" 조건으로 F7 해소.
  - 브라우저 검증: 상수 3대 → **RPUW16BX9M 1대(46.4kW · 연결 7대/6실 · 조합비 0.81 · 허용 0.50~1.03)**, 미배정 0. 테스트 876 그린.
- [x] **P3. 스텝 순서 교체 + StepGuard** (2026-07-10, 미커밋, 주인님 지시: "전제 안 하면 다음 스텝으로 안 넘어가는 주의 팝업")
  - **스텝 순서 교체**: `검출 → 실내기 배치 → 실외기 선정·조합 → 실외기 배치 → 산출물`. 예전엔 배치(3)가 조합(4)보다 앞인데 실제로는 배치 단계 진입 시 조합을 먼저 돌렸다(라벨과 실행 순서가 반대).
  - `domain/generation/StepGuard.ts`(순수) — `guardAdvance`/`guardRegress`/`guardDestructive`. 판정은 **BLOCK**(진행 불가) / **CONFIRM**(확인 후 진행) / ALLOW. 모든 BLOCK은 `title` + `reason`(왜) + `remedy`(어떻게 푸는지)를 갖는다(테스트로 강제).
  - **파이프라인 어휘(`StepId`·`STEP_ORDER`)를 도메인으로 승격** — 도메인이 presentation을 import할 수 없다(§5.1 규칙 1). `steps.ts`는 라벨·번호만 갖는다.
  - **UI 정책: CTA는 항상 활성.** 클릭하면 `GuardModal`이 이유를 말한다. 예전 `place→outdoor` 버튼은 조건 미충족 시 아예 렌더되지 않아 사용자가 왜 못 가는지 몰랐다.
  - **실외기 심볼도 controlled 전환** — 가드가 '몇 대 중 몇 대 배치됐는지' 알아야 한다. `outdoorPositions`(그룹 key → 좌표)를 App이 소유. 그룹이 사라지면 좌표도 정리.
  - **시설군 잠금 해제** → `FACILITY_CHANGE` 확인 팝업. 잠그면 왜 못 바꾸는지·어떻게 바꾸는지 알 수 없었다.
  - `beforeunload` 이탈 방지(배치 진행 중일 때만).
  - 브라우저 검증: 배치 없이 선정 시도 → *"실 6곳(거실 · 침실1 …)에 실내기가 없습니다. 실내기 정격용량이 확정돼야 …"* + 해결법. 실외기 미배치로 산출물 시도 → *"실외기 1대 중 0대만 도면에 배치됐습니다. 미배치: 실외기-1"*. 테스트 915 그린.
- [x] **P4. 실외기 좌표를 산출물에 반영 + 이격거리** (2026-07-10, 미커밋)
  - **결함: '실외기 배치' 스텝이 어떤 산출물에도 기여하지 않았다.** `buildDrawingSvg(rooms, indoorByRoom, groups)`에 좌표 인자가 없어, 실외기는 하단 `PAD + i*150`에 다시 그려지고 실내기는 실 중심에 다시 그려졌다. 뷰어에서 아무리 옮겨도 산출 도면은 동일했다.
  - `buildDrawingSvg(DrawingInput)`으로 교체 — `indoorSymbols`(좌표·회전) + `outdoorPositions`를 받아 **놓인 그대로** 그린다. 좌표 없는(미배치) 실외기와 빈 그룹은 제외. viewBox가 도면 밖 실외기까지 담는다.
  - `domain/generation/clearanceRules.ts`(순수) — 본체 1240×760mm. **범위는 실외기끼리의 간격만**(주인님 확정): 좌우 200 이상이거나 앞뒤 900 이상이면 OK, 둘 다 못 지키면 위반. 측250·후500(벽 기준)은 벽·장애물 정보가 없어 보류. 겹침은 두 gap이 음수라 자동 위반.
  - 좌표는 뷰어 정규화 단위 → `mmPerUnit`으로 mm 환산 후 검사(타일 없는 목업 좌표계에서는 실치수를 몰라 검사 생략).
  - 브라우저 검증: 실외기를 `(160,480) → (260,500)`으로 드래그 후 `도면.svg` 재생성 → `translate(260, 500)`. 실내기 심볼 7개(대수와 일치)도 각자 좌표로. 테스트 933 그린.
  - 잔여: 이격거리 위반 시 `CONFIRM` 팝업이 뜨는 경로는 실도면 타일이 있어야 실측 검증 가능(목업 좌표계는 스킵).
- [x] **P5. UI 전면 재편** (2026-07-10, 미커밋) — 도면이 주인공이 되도록 크롬을 걷어냈다.
  - **상단 크롬 210px → 96px** (실측). 제목 바(44) 삭제 — 제목·카운터는 스텝퍼가 이미 말한다. 스텝퍼(40) + 리포트 액션(40) + 툴바(38)를 `WorkBar`(48) 하나로 통합. 뷰어 세로 1061 → 1147px.
  - **KPI는 하단 `StatusBar`(28px)로.** 조합 리포트는 '읽는 값'이고 CTA는 '누르는 것'이라 한 줄에 섞을 이유가 없었다(SRP). `ReportStrip` 삭제.
  - **`⭳ 결과 다운로드` 제거**(F9) — 검출 단계부터 상시 노출돼 파이프라인을 우회했다. 캡처는 `⋯` 오버플로 메뉴로. 레이어 셀렉트는 뷰어 좌상단 도구로(툴바 밴드 소멸).
  - **조합 매핑: 전체화면 모달 → 하단 도킹 패널**(`MappingDock`, 높이 조절). 도면 위 실내기 심볼을 보면서 조합한다. 덤으로 `ROOMS` 직접 import 제거(백로그 해소) — 칩이 이제 **정격**용량을 표시해 조합비와 기준이 같다(예전엔 설계부하라 숫자가 안 맞아 보였다).
  - **스텝별 컨텍스트 패널**: `DetectPanel`(검출 결과) / `ModelPanel`(배치·조합) / `OutdoorPanel`(배치 체크리스트 + 이격거리 경고) / `OutputPanel`(산출물). `PanelShell`로 접기·폭조절 공통화.
  - **`StepOverlay` 삭제** — 산출물 단계가 뷰어를 통째로 덮어 도면을 보며 내려받을 수 없었다. 이제 도면은 모든 단계에서 살아 있다.
  - 뷰어 정리: `.vhint`(상단 중앙 텍스트) 삭제 — 우상단 단축키 위젯과 중복. 위젯은 기본 접힘.
  - SRP 분해: `useTileManifest` / `useSelectionSync` / `useScheduleSync` 추출. App.tsx 1023 → 956줄.
  - 문서 동기화: `POC/CLAUDE.md §3`(파이프라인 순서·가드·도킹), 용어집(실내기=1대, 대수, 조합비 정격 기준, maxConnections=대수), `doc/05_설계결정/실외기_선정_룰.md` 신규, `doc/index.md`.
  - 브라우저 검증: 5스텝 완주 · 콘솔 에러 0 · 산출물 3종. 테스트 940 그린.
  - 잔여: App.tsx 956줄은 여전히 크다(`usePipelineState` 추출 후보). 줌 컨트롤은 뷰어에 남겨 둠(상태바로 올리면 휠마다 App이 리렌더).

## 2026-07-10

- [x] **자동배치 룰 1/N — 단위부하표 + 프로젝트 시설군** (2026-07-10, 주인님 지시)
  - 근거 자료 확보: [`doc/03_데이터/LG전자_단위부하_참고자료.pdf`](03_데이터/LG전자_단위부하_참고자료.pdf) (주인님 제공). Confluence 표는 이 자료의 Standard 열 요약본이었다 → **원본이 우선**.
  - **발견 1: 같은 실명도 시설군마다 값이 다르다** (식당: 주거 120 / 상업 210, 로비: 숙박 180 / 종교 150). 실명만으로는 부하를 정할 수 없다 → 시설군이 전제.
  - **발견 2: 특수부하 칸이 숫자가 아닌 행이 있다** (연회장 '예식장', 판매장 '대형매장', 공장 '발열기기 별도'). 숫자로 파싱하면 안 되므로 `specialNote`로 분리.
  - **발견 3: 동의어는 표에 없을 때만 적용해야 한다.** '세미나실'은 숙박시설 표에 180으로 실재 — 회의실(150)로 덮으면 원표 값을 잃는다(회귀 테스트로 고정).
  - `unitLoadTable.ts`: 7 시설군 × 실명 × (Standard/저부하/고부하/특수부하). 없는 강도는 Standard로 폴백, 없는 실명은 150.
  - `Room`에 `facility`·`intensity` 추가. `withIntensity`(지하층=저부하, 외기 2면=고부하, 천정고 4m 이상=특수부하)는 배선만 해둠.
  - **시설군은 프로젝트 설정**(주인님 지시): 생성 첫 화면(실 검출 단계)에서 선택, 검출 후 잠금. 브라우저 검증: 같은 도면이 OFFICE 32.8kW / 주거시설 31.6kW.
  - 옛 목업 단위부하(거실 170 등) 폐기. 엑셀 시나리오 테스트는 실측 단위부하를 명시(`aiUnitLoad`)해 보존.
  - **남은 것**: 타입 결정(짧은 폭 4m·2WAY 3m·단위세대 1WAY) · 대수 결정(부하 vs 확산범위) · 모델 선정(부하 충족 최소 용량 + 한 방 동일 용량) · 40HP 분할 · 층 규칙. ⚠️ 타입/대수는 실 폭·길이(m)가 필요한데 목업은 SVG 픽셀뿐 → 실측 치수 부여 선행.

- [x] **S3. 조합비 정책 UI** (2026-07-10, 주인님 지시) — 이틀 묵은 블로커(전역 상한 1.0 vs 1.3) 해소.
  - **전역 기본 50%~103% 확정**(주인님). 근거: Confluence「자동배치 룰」 "목표 100%, 허용 50~103%. 103% 초과=용량부족(over), 50% 미만=과대(low)". 선정표 주석 "100% 이내"·기존 코드 1.3과 상충했으나 회의 확정값을 따른다. `ComboRange.DEFAULT` → (0.5, 1.03).
  - **override 단위는 제품군이 아니라 실외기 모델별**(주인님 지시 — 계획서 §3.5의 제품군별보다 세밀). `ComboPolicy` VO(전역 + 모델별, 우선순위: override > 전역).
  - 저장: `system_settings`(combo.ratio.min/max) + `products.combo_min/combo_max`. SCHEMA_VERSION 4.
  - 전파: `SqliteEquipmentMaster.readPublishedOutdoor`가 정책을 해석해 스펙에 실어 보냄 → 생성단 조합비 경고선이 설정을 즉시 따른다. 인메모리 마스터도 전역 기본을 실어 두 백엔드 계약 동치 유지.
  - **정책은 스펙이 아니다** → 게시본(PUBLISHED)도 조합비 조정 가능(스펙 잠금 SPEC_LOCKED 미적용).
  - 실데이터 GHP 1.106은 전역 기본(1.03)에서 과부하 → 모델별 override가 필요한 이유(테스트로 고정).
  - UI: 사이드 메뉴 '조합비 정책' 신설. 전역 폼 + 실외기 표(계열·시리즈·상태·**적용 출처** 필터, 검색, 편집/기본값으로).
  - 브라우저 검증: RPUW12BX9M에 override 50~90% 저장 → 매핑 팝업이 모델별로 `허용 0.50~0.90` / 전역 `0.50~1.03` 표기.
  - 잔여: 제품군(중분류) 단위 override는 미도입(모델별로 충분). MappingModal의 `ROOMS` 직접 import는 여전히 백로그.
- [x] **관리 화면 탐색 재배치** (2026-07-10, 주인님 지시) — 검색은 버튼·Enter 제출로 전환(입력마다 1,206행 재필터 안 함), 검색 바(검색·초기화) 1행 + 필터 바 2행. 장비 목록관리·조합비 정책 동일 구조. 페이지 설명문 제거(AI 티 나는 안내 문구).

- [x] **마력(HP) 환산식 적용 + 백필** (2026-07-10, 주인님 지시) → [`doc/05_설계결정/마력_환산식_적용_검토.md`](05_설계결정/마력_환산식_적용_검토.md)
  - Confluence 0708 확정 환산식(1HP = 2500 kcal/h = 2907 W) 도메인화(`shared/Horsepower.ts`). 원문 자기모순(`2900w` vs `2907W`) → 2907 채택(주인님).
  - **환산식은 산출식이 아니라 검증·백필식**임을 실데이터 670건 전수 대조로 확인(정수 일치 최대 66.9%, 계열별 W/HP 2,813~2,900). HP 1차 출처는 모델명 유도 유지.
  - **결함 발견·수정**: 100HP 이상 모델 12건을 10HP로 오독(`RP-Q1001X9S` 등, 접두 뒤 2자리만 읽음). 냉방용량 환산으로 100HP대를 판별하도록 `horsepowerFromModelCode(model, coolingW?)` 확장. 전부 DRAFT였어 생성단 무영향.
  - **백필 179건**: 비-VRF(칠러·CDU·단품)는 모델명 숫자가 용량이라 유도 금지 → `HP = round(coolingW/2907)`. 1HP 미만 1건(`LSC-G0100F2`)은 소수 보존(0.34, 주인님 지시).
  - **`is_vrf`를 시리즈 테이블로 승격**(구 `derivesHp`): `energy_source`로는 못 가른다(수냉식에 VRF·칠러 공존, EHP에 Multi V·단품 공존). 이 한 축이 ①모델명 HP 인코딩 ②maxConn 게시 요건 ③생성단 조합 후보 노출을 함께 가른다.
  - **게시 요건 계열별 분리**: HP만 백필하면 게시가 0건 풀린다(179건 전부 maxConn 없음). 비-VRF는 maxConn 면제(주인님 확정), 대신 `publishedOutdoor()`가 VRF만 노출해 생성단 격리.
  - `hp_source`(MODEL_CODE/DERIVED/CURATED/MANUAL) 기록 + 관리 목록 `22 (추정)` 표기. SCHEMA_VERSION 3.
  - 브라우저 실검증: 칠러 게시(23→24) 후에도 생성단 실외기 카탈로그 VRF 7종만. 테스트 634 그린.
  - 잔여: 실외기 16건은 냉방용량이 없어 백필 불가(게시 계속 차단). CDU 3건은 시트에 마력이 있어 `hp_source='SHEET'` 도입 시 시트값 우선이 정확.

## 2026-07-09 예정 (2026-07-08 주인님 지시)

- [x] **장비선정표 데이터 소스를 장비마스터 참조로 전환** (2026-07-09, 미커밋) — Equipment Master 컨텍스트 신설(`domain/equipment`: PublishStatus·MasterRecord·EquipmentMaster 포트, `infrastructure/equipment/InMemoryEquipmentMaster` = 시드 SSOT + 게시 게이트). 실내기 SEED·실외기 ODU_CATALOG를 마스터로 이관(PUBLISHED), 게이트 실증용 DRAFT/ARCHIVED 1건씩 추가. 생성 카탈로그(Indoor/Outdoor)를 마스터 참조 어댑터로 전환(기본 싱글턴 주입 → 기존 호출부 유지). App 컴포지션 루트에서 마스터 1개 주입. 마스터는 PUBLISHED만 노출(생성/검도 참조). 테스트 387개 그린.

## 2026-07-09 신규 (2026-07-09 주인님 지시)

- **장비마스터(SQLite 내장) — 구현 계획서 작성 완료** → [`doc/장비마스터_구현계획.md`](장비마스터_구현계획.md) (2026-07-09). 스택 확정: 브라우저 내장 sql.js(WASM)+IndexedDB. 입력=LG 스펙시트, 출력=선정표/일람표(hot 필드), 구조=schema_v2.sql 서브셋(4단 분류+products 정규화+product_specs JSONB+게시게이트).
  - [x] **P1. 읽기 백엔드 교체** (2026-07-09, 미커밋) — sql.js(WASM)+IndexedDB 저장소·SQLite 스키마(4단 서브셋+뷰)·정규화 시드(공유 seedData 16+7)·`SqliteEquipmentMaster`(PUBLISHED 스냅샷 materialize)로 생성단 무영향 교체. `main.tsx` 부트스트랩 주입(App `master` 옵셔널 prop, 기본 인메모리→테스트/폴백). **동치 테스트로 SQLite≡InMemory 고정**. 브라우저 실구동 검증(IndexedDB 생성·16 실내기 UI 흐름·콘솔 에러 0). 테스트 396개. 적대적 리뷰(26 에이전트) 확정 3결함 반영: ①캐시 무효화 SEED_HASH(시드 내용 해시) 키 ②부트스트랩 타임아웃 레이스+로딩 표시 ③순서/시드드리프트 테스트. 후속(P2): 옛 IndexedDB 키 고아 청소.
  - **P2. 쓰기 포트 + 관리 페이지** — 세로 슬라이스로 진행:
    - [x] **S1. 관리 목록(읽기)** (2026-07-09, 미커밋) — `EquipmentAdminRepository.listProducts`(전 상태) + SQLite 어댑터, 관리 페이지(목록/분류·상태 필터/검색/페이지네이션/상태뱃지, 무채색), `?view=equipment` 라우팅 + GNB 링크, `query.ts` 공용 헬퍼 추출. 브라우저 검증(25제품 전 상태). 적대적 리뷰(12에이전트)→접근성 aria-label 2건 반영. 테스트 405.
    - [x] **S2. 쓰기 포트 + 등록/수정/게시** (2026-07-09, `15fc1e0`) — 상태전이 불변식(선형+재게시, PUBLISHED→DRAFT 금지)·게시본 스펙 잠금(DRAFT만 수정)·`ProductDraft` VO 자기검증·`EquipmentDomainError(code)`. SQLite 쓰기(create/update/setStatus, BEGIN/COMMIT/ROLLBACK) + 성공 커밋 후 IndexedDB 영속. 등록/수정 폼·행별 전이 액션. `useSubmitGuard`로 더블클릭 방지(주인님 지시) — StrictMode 이중 마운트에서 busy가 영구 true로 굳던 결함을 브라우저 검증에서 발견·회귀 고정. 테스트 476.
    - [x] **단가 제거** (2026-07-09, `35c7d38`, 주인님 지시) — 스펙시트에 단가가 없어 관리 UI 단가 모달·setPrice·단가 컬럼, 생성 카드 `mp`·매핑팝업 단가·일람표 CSV 단가 컬럼 제거. 스키마 `product_prices`·도메인 `Price` VO는 존치.
    - [ ] **S3. 조합비 정책 UI** — 전역+제품군별 min/max 설정(계획서 §3.5) → 생성단 전파. ⚠️ 착수 전 전역 기본 상한(1.0 vs 1.3) 확정 필요.
  - [x] **P3. 스펙시트 업로드 등록** (2026-07-09, `2ab7a12`) — LG 원본 xlsx 직파싱(전치형, 헤더행 자동탐지). 정규화(냉·난방 정격 kW→W, 최대연결수) + 롱테일 전량 `product_specs` JSONB 보존. **HP는 모델명에서 유도**(접두 뒤 첫 2자리; kW÷상수 변환식은 계열별로 성립하지 않음 — 493모델 전수 확인). `classifyImport` 정상/오류/중복 → 업로드 모달(Figma ADM-EQP-001 상세2 참고, 템플릿 다운로드 단계 제외) → OK 행만 DRAFT 일괄 적재(단일 트랜잭션). 브라우저 실검증(4모델 적재·영속·롱테일 저장). 테스트 519.
  - [x] **운영정책서 작성** (2026-07-09, 주인님 지시) → [`doc/장비마스터_운영정책서.md`](장비마스터_운영정책서.md)
  - [ ] **P3-후속. 라벨 정규화 사전 + import_jobs** — `spec_label_aliases`(제품군별 라벨 → 표준 키), 업로드 이력·거부로그 테이블. 현재는 라벨 원문을 그대로 JSONB 키로 사용.
  - [ ] **P4. 실데이터 적재 + 산출물 확장** — 50종 스펙시트 적재(시리즈·중분류 코드 체계 확정 선행), `product_specs` 키 → 일람표 컬럼 매핑표 작성 후 선정표/일람표 실제 컬럼 연결.
  - [ ] **조합비 정책 UI 설정** (주인님 결정 2026-07-09) — 100% vs 0.5~1.3 상충을 하드코딩 대신 관리 UI 설정으로 해소. 전역 기본(system_settings) + 제품군별 override, `ComboRange` VO로 운반, 생성단 경고선 즉시 전파(계획서 §3.5). P2(관리 페이지)에 포함.
  - ⚠️ 남은 확인(계획서 §8): 조합비 전역 기본 초기값(1.0 vs 1.3), P1 시드 범위, 일람표 세부필드 우선순위, 4단 분류 코드 확정, 영속 정책.
- [x] **조합 리포트 초기 상태 초기화** (2026-07-09, 미커밋) — `domainRooms`를 빈 상태로 시작 → '실 검출 실행'이 채우도록 변경(총부하가 검출 후에만 뜸). `INITIAL_GROUPS.items`/`INITIAL_POOL` 사전배정 제거. `ReportStrip` cover NaN 가드. 검출 전 전 지표 0/빈 확인 테스트 추가(`App.report.test.tsx`).
- [x] **미배정 카운트 상수 1 제거** (2026-07-09, 미커밋) — `INITIAL_POOL=[]`. 배정은 파이프라인 결과로만: 실내기 배치(`aiPlace`) 시 전 실을 미배정 풀에 편입(`ensureRoomsInPool`) → 미배정 6, combine 진입 시 `DEFAULT_COMBINATION` 자동 조합 1회 적용(`autoCombine`) → 배정 6/미배정 0, 이후 매핑 팝업에서 사용자 조정. (combine UX 방향: 주인님 선택 "자동 조합 기본값")

- [x] **실내기 자동배치 룰** (2026-07-10, 미커밋) — 타입 결정(형상·부하·단위세대·복도) + 확산범위 기준 대수 + 동일용량 선정. `Room`에 실측 치수(`shortSideM`/`longSideM`) 필수화, `aiSelectionFor`로 규칙 단일화(호출부 3곳 통합). 근소 부족 허용 **3%** 확정(주인님) — 엑셀 실측 1.72%와 0708 예시 5.26% 사이. 카탈로그 유형 라벨 정정(C=1WAY·T=4WAY) + 2WAY 3종 신규 게시(16→19종). → [`doc/05_설계결정/실내기_자동배치_룰.md`](05_설계결정/실내기_자동배치_룰.md). 테스트 818.

## 백로그 (미확정·후속)

- **`requireHeating`을 프로젝트 설정으로 승격** — 지금은 항상 true 하드코딩. 냉방전용 건물을 다루려면 시설군과 같은 층위의 설정이어야 한다.
- **App.tsx 956줄** — `usePipelineState`(step·placements·plan·가드) 추출 후보.
- 이격거리 CONFIRM 경로는 실도면 타일이 있어야 실측 검증 가능(목업 정규화 좌표계는 mmPerUnit이 없어 검사 생략).
- 층별 그룹핑은 구현됐으나 목업이 지상1층 단일이라 실검증 미완.
- 실외기 배치 좌표를 DXF export로 왕복(현재 SVG 대체 산출물).
- 자동배치 후속: 장비번호 자동 채번(1WAY `C`·2WAY `G`·4WAY `T`·스탠드 `P`·실외기=마력)
- 2WAY 최소 용량이 5.2kW라 좁고 긴 소규모 실(탕비실 12㎡ · 필요 2.1kW)이 과대선정됨 — 룰과 실물 라인업의 귀결, 보정 여부 미정
- SQLite 캐시 복구: IndexedDB에 저장된 바이트가 열리지 않으면 캐시를 버리고 시드에서 재적재 (현재는 초기화 실패로 폴백 → 관리 페이지가 뜨지 않음)

- 조합비 제품군별 실정책 수치 확정 (현재 전부 기본 0.5~1.03 — 모델별 override 구조는 마련됨)
- 실내기 난방용량 실측치 반영 (13종은 ×1.10~1.13 보간 목업), 실외기 heatKw도 ×1.12 근사
- ~~MappingModal이 `ROOMS` 직접 import~~ → P5에서 해소(`MappingDock`이 실 정보를 prop으로 받는다)
- 비고(remarks) 컬럼 편집, xlsx 직접 출력(현재 CSV+BOM), 다층(층 다중화) 시나리오
- 적대적 QA 패스 보강 (07-08 세션 한도로 1회 중단됨)
- 우측 패널 실내기 100+ 대량 항목 스크롤 대응
- 실 검출 정합·DXF export (딥줌 타일 뷰어 후속)
